{
  "name": "master-microfrontend-shell",
  "version": "1.0.0",
  "private": true,
  "description": "Master Micro-Frontend Shell - Monolithic Build",
  "scripts": {
    "install:all": "pnpm install -r",
    "dev": "pnpm --filter angular-shell dev:serve & pnpm --filter scramble-words dev & pnpm --filter life-goals dev & pnpm --filter quiz-game dev & pnpm --filter life-milestone-race dev",
    "dev:shell": "pnpm --filter angular-shell dev:serve",
    "dev:games": "pnpm --filter scramble-words dev & pnpm --filter life-goals dev & pnpm --filter quiz-game dev & pnpm --filter life-milestone-race dev",
    "build": "pnpm run build:games && pnpm run build:shell",
    "build:games": "pnpm --filter scramble-words build && pnpm --filter life-goals build && pnpm --filter quiz-game build && pnpm --filter life-milestone-race build && pnpm run copy:games",
    "build:shell": "pnpm --filter angular-shell build",
    "copy:games": "node scripts/copy-games.js",
    "serve": "pnpm --filter angular-shell serve",
    "serve:prod": "pnpm --filter angular-shell serve:prod",
    "preview": "pnpm --filter angular-shell preview",
    "clean": "pnpm -r exec rm -rf dist node_modules",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "fs-extra": "^11.2.0"
  },
  "dependencies": {
    "pnpm": "^10.29.3"
  }
}